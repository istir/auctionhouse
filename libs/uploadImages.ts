export default async function uploadImages(images:Blob[]) {
    // const fReader = new FileReader()
    // images.forEach(image => {
    //     fReader.readAsDataURL(image)  
    //     fReader.onloadend
    //     if(typeof event.target?.result==="string")  
    // });
    
}