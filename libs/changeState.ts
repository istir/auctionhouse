// export default function changeState(
//   setState: any,
//   stateElement: boolean,
//   explicitStatus?: "open" | "close"
// ): void {
//   // changeSearchBoxState(): void {
//   //TODO make so that it has an animation on open

//   if (explicitStatus) {
//     setState({ searchOpen: explicitStatus === "open" ? true : false });

//     return;
//   } else {
//     setState((prevState) => ({ searchOpen: !prevState.searchOpen }));
//   }
// }
